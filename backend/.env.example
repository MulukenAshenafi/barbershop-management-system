# =============================================================================
# BARBERSHOP MANAGEMENT SYSTEM - ENVIRONMENT VARIABLES
# =============================================================================
# 
# INSTRUCTIONS:
# 1. Copy this file to .env in the backend directory
# 2. Replace all placeholder values with your actual credentials
# 3. NEVER commit .env to version control (it's in .gitignore)
# 4. Keep your .env file secure and never share it publicly
#
# =============================================================================

# -----------------------------------------------------------------------------
# DJANGO CORE SETTINGS
# -----------------------------------------------------------------------------

# SECRET_KEY: Django secret key for cryptographic signing
# Generate a new one using: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# IMPORTANT: Use a strong, unique key in production!
SECRET_KEY=django-insecure-change-this-in-production-generate-a-new-secret-key

# DEBUG: Enable/disable debug mode
# Set to False in production for security
# When True: Shows detailed error pages, allows ALLOWED_HOSTS='*'
# When False: Hides errors, requires specific ALLOWED_HOSTS
DEBUG=True

# ALLOWED_HOSTS: Comma-separated list of host/domain names this site can serve
# For development: localhost,127.0.0.1
# For production: your-domain.com,www.your-domain.com
# Note: If DEBUG=True, this can be '*' (not recommended for production)
ALLOWED_HOSTS=localhost,127.0.0.1,192.168.1.100

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION (PostgreSQL)
# -----------------------------------------------------------------------------

# DB_NAME: Name of your PostgreSQL database
# Create this database first: CREATE DATABASE barbershop_db;
DB_NAME=barbershop_db

# DB_USER: PostgreSQL username
# Default is 'postgres', but you can create a dedicated user
DB_USER=postgres

# DB_PASSWORD: PostgreSQL password
# Set this to your PostgreSQL user's password
DB_PASSWORD=postgres

# DB_HOST: Database server hostname
# Use 'localhost' for local development
# Use IP address or domain for remote databases
DB_HOST=localhost

# DB_PORT: PostgreSQL port number
# Default PostgreSQL port is 5432
DB_PORT=5432

# -----------------------------------------------------------------------------
# CLOUDINARY CONFIGURATION (Image Storage)
# -----------------------------------------------------------------------------
# Cloudinary is used for storing user profile pictures, product images, and service images
# Sign up at https://cloudinary.com/ to get your credentials

# CLOUDINARY_NAME: Your Cloudinary cloud name
# Found in your Cloudinary dashboard: https://console.cloudinary.com/
CLOUDINARY_NAME=your-cloudinary-cloud-name

# CLOUDINARY_API_KEY: Your Cloudinary API key
# Found in your Cloudinary dashboard under Account Details
CLOUDINARY_API_KEY=your-cloudinary-api-key

# CLOUDINARY_SECRET: Your Cloudinary API secret
# Found in your Cloudinary dashboard under Account Details
# Keep this secret! Never expose it in client-side code
CLOUDINARY_SECRET=your-cloudinary-api-secret

# -----------------------------------------------------------------------------
# CHAPA PAYMENT GATEWAY CONFIGURATION
# -----------------------------------------------------------------------------
# Chapa is an Ethiopian payment gateway
# Sign up at https://chapa.co/ to get your credentials
# These are placeholders - replace with actual Chapa credentials when integrating

# CHAPA_SECRET_KEY: Your Chapa secret key for server-side operations
# Found in your Chapa dashboard under API Keys
CHAPA_SECRET_KEY=your-chapa-secret-key

# CHAPA_PUBLIC_KEY: Your Chapa public key for client-side operations
# Found in your Chapa dashboard under API Keys
CHAPA_PUBLIC_KEY=your-chapa-public-key

# CHAPA_WEBHOOK_SECRET: Secret for verifying webhook requests from Chapa
# Set this in your Chapa dashboard webhook settings
# Used to verify that webhook requests are actually from Chapa
CHAPA_WEBHOOK_SECRET=your-chapa-webhook-secret

# -----------------------------------------------------------------------------
# FIREBASE CONFIGURATION (Social Login - Placeholder)
# -----------------------------------------------------------------------------
# Firebase is used for social authentication (Google, Facebook, etc.)
# These are placeholders - configure when implementing Firebase authentication

# FIREBASE_PROJECT_ID: Your Firebase project ID
# Found in Firebase Console: https://console.firebase.google.com/
# Project Settings > General > Project ID
FIREBASE_PROJECT_ID=your-firebase-project-id

# FIREBASE_CREDENTIALS: Path to Firebase service account JSON file
# OR: Base64-encoded JSON credentials
# Download from Firebase Console: Project Settings > Service Accounts
# Example: /path/to/firebase-credentials.json
# OR: base64_encoded_json_string_here
FIREBASE_CREDENTIALS=your-firebase-credentials-json-path-or-base64-string

# -----------------------------------------------------------------------------
# OPTIONAL: SERVER CONFIGURATION
# -----------------------------------------------------------------------------

# PORT: Port number for Django development server (optional)
# Default is 8000, but you can override if needed
# Only used if you modify manage.py to read this
# PORT=8000

# -----------------------------------------------------------------------------
# PRODUCTION SETTINGS (When deploying)
# -----------------------------------------------------------------------------
# When deploying to production, make sure to:
# 1. Set DEBUG=False
# 2. Set ALLOWED_HOSTS to your actual domain
# 3. Use a strong SECRET_KEY (generate new one)
# 4. Use secure database credentials
# 5. Enable HTTPS/SSL
# 6. Set up proper CORS origins
# 7. Configure proper static file serving
# 8. Set up proper logging
# 9. Use environment-specific settings files

# -----------------------------------------------------------------------------
# QUICK SETUP CHECKLIST
# -----------------------------------------------------------------------------
# [ ] PostgreSQL database created
# [ ] Database credentials configured
# [ ] Cloudinary account created and credentials added
# [ ] Chapa account created (when ready for payment integration)
# [ ] Firebase project created (when ready for social login)
# [ ] SECRET_KEY generated and set
# [ ] DEBUG set appropriately for your environment
# [ ] ALLOWED_HOSTS configured for your domain/IP

# -----------------------------------------------------------------------------
# NOTES
# -----------------------------------------------------------------------------
# - All values should be in quotes if they contain special characters
# - No spaces around the '=' sign
# - Empty values are allowed but may cause errors
# - Comments (lines starting with #) are ignored
# - Use single quotes or no quotes for simple values
# - For multi-line values, use quotes and \n for newlines
