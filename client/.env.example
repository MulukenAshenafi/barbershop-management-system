# API base URL (no trailing slash). Required when using a physical device.
# Use your machine's LAN IP so the device can reach the backend (e.g. from Metro: exp://172.20.30.59:8081 -> use 172.20.30.59).
# EXPO_PUBLIC_API_URL=http://172.20.30.59:8000/api

# Optional: Google OAuth Web Client ID for "Continue with Google" on Welcome screen.
# EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Optional: Force app to start at Welcome screen every time (ignores stored login). Set to true for testing.
# EXPO_PUBLIC_FORCE_WELCOME=true
