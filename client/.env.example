# API base URL (no trailing slash). Required when using a physical device.
# EXPO_PUBLIC_API_URL=http://10.205.206.9:8000/api
# Backend must listen on 0.0.0.0 (e.g. python manage.py runserver 0.0.0.0:8000 or docker compose up).

# Firebase (Auth only – no Firestore/Realtime DB). From Firebase Console > Project settings > General.
# EXPO_PUBLIC_FIREBASE_API_KEY=your-api-key
# EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
# EXPO_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
# EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
# EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
# EXPO_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abc

# Optional: Google OAuth Web Client ID for "Continue with Google".
# EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Optional: Email sign-in link URL (must be whitelisted in Firebase Console > Auth > Sign-in method > Email link).
# Default: barberbook://email-signin (native) or current origin + /email-signin (web).
# EXPO_PUBLIC_EMAIL_LINK_URL=barberbook://email-signin

# Optional: Force app to start at Welcome screen every time (ignores stored login).
# EXPO_PUBLIC_FORCE_WELCOME=true
