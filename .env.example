# Docker Compose and the backend container both use .env from PROJECT ROOT.
# Copy this to .env at project root (same folder as docker-compose.yml).
# Edit values; this file is the single source for Docker.
DB_NAME=barbershop_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_PORT=5432
